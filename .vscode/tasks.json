{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build System",
            "type": "shell",
            "command": "sudo",
            "args": [
                "nixos-rebuild",
                "switch",
                "--flake",
                ".#${input:host}",
                "--target-host",
                "${env:USER}@${input:host}",
                "--build-host",
                "${env:USER}@${input:host}"
            ],
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ],
    "inputs": [
        {
            "id": "host",
            "type": "command",
            "command": "shellCommand.execute",
            "args": {
                "command": "find ./hosts -maxdepth 2 -mindepth 2 -exec basename {} \\; | sed -e \"/$HOSTNAME/d\" -e \"1i$HOSTNAME\"",
                "cwd": "${workspaceFolder}",
                "description": "Target Host"
            }
        }
    ]
}